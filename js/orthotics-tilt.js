/* ============================================================
   3D ORTHOTICS: Gyroscope tilt on orthotic portfolio cards
   DISABLED — we don't want an iOS permission pill appearing on
   phones just to offer a subtle tilt effect. File left in place
   so older cached HTML that still references it doesn't 404.
   ============================================================ */
(function () { /* intentionally empty */ })();
